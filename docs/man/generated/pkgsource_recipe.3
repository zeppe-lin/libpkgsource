.\" Generated from docs/man/pkgsource_recipe.3.md; do not edit.
.\"
.TH "PKGSOURCE_RECIPE" "3" "" "libpkgsource 3.0.1" "libpkgsource"
.SH NAME
pkgsource_recipe \- seal normalized native recipe authority
.SH SYNOPSIS
.IP
.EX
#include <libpkgsource/recipe.h>
.EE
.SH DESCRIPTION
\f[CR]pkgsource::recipe_declaration\f[R] is parser\-neutral input.
It contains package release, metadata, source inputs, one exact build program, an optional exact check program, requirement declarations, lifecycle programs, architecture requirements, and declaration provenance.
.PP
\f[CR]pkgsource::seal_recipe()\f[R] performs deterministic normalization.
It sorts source and lifecycle domains, rejects duplicate source local names and lifecycle actions, expands package and profile requirements through one sealed profile catalog, rejects lifecycle requirements without a program for the same action, and rejects check requirements without a check program.
.PP
A check program with no additional check requirements is valid.
.PP
The returned \f[CR]pkgsource::sealed_recipe\f[R] retains normalized semantic fields, selected build profiles, the exact profile closure, all resolved requirement origins, and diagnostic provenance.
Its public reconstruction constructors verify canonical source/lifecycle ordering and the same lifecycle/check closure invariants before admitting the value.
It has no independent recipe identity; the complete normalized recipe contributes directly to \f[CR]pkgsource::source_snapshot_identity\f[R].
.SH BOUNDARY
Recipe declarations and sealed recipes contain no parser generation or input syntax tag.
Parsing, acquisition, resolution, fetching, execution, and planning are outside the core.
.SH SEE ALSO
\f[CR]libpkgsource(3)\f[R], \f[CR]pkgsource_profile(3)\f[R], \f[CR]pkgsource_snapshot(3)\f[R]
